import React from 'react';

interface PlayerHeroProps {
    playerName?: string;
    username: string;
    teamName?: string;
    leagueName?: string;
    imageUrl?: string;
}

export function PlayerHero({
    playerName,
    username,
    teamName,
    leagueName,
    imageUrl
}: PlayerHeroProps) {
    return (
        <div class
    );
}